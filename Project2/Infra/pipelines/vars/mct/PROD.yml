variables:
  resourceGroup: "mct-rg-prod" #Deploy env resource group
  azureSubscription: "mct-prod" #Deploy env DevOps service Connection
  env: "PROD"
  location: "westus" #Default location for deployment
  dateTime: $(Get-Date -Format yyyyMMddhhmmss)