import { defineMessages } from 'react-intl';

export const messages = defineMessages({
    btnCloseMessageBar: {
        id: 'oseExpandableMessage.btnCloseMessageBar',
        defaultMessage: 'Close MessageBar'
    },

});