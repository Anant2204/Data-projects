import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  searchPlaceholderText: {
    id: 'oseGrid.searchPlaceholderText',
    defaultMessage: 'Search in all columns'
  },
});
